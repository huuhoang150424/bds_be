export enum Roles {
  Admin = "Admin",
  Agent = "Agent",
  User = "User",
}

export enum UserAction {
  LOCK = 'LOCK',
  UNLOCK = 'UNLOCK',
}

export enum Gender {
  Male = "Male",
  Female = "Female",
  Other = "Other",
}